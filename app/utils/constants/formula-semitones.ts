import { Scale } from '@/models/scale.type';

export const formulaSemitones: Record<Scale, [number, number, number, number, number, number, number]> = {
  maj: [2, 2, 1, 2, 2, 2, 1],
  min: [2, 1, 2, 2, 1, 2, 2],
};
